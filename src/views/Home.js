import React, { useContext } from 'react'
import Posts from '../components/unique/Posts/Posts'

const Home = () => {
  return (
    <main className="home container">
      <Posts />
    </main>
  )
}

export default Home
